const compare = document.querySelector('#compare1');//any css selector

var fullSched = document.querySelector("#your-month")

fullSched.addEventListener
compare.addEventListener("click",(e))=>{
  fullSched.width = "0"
  fullSched.height = "0"
}
